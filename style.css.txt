/* General Reset */
* { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; }
body { font-family: 'Arial', sans-serif; line-height: 1.6; color: #333; background: #fff8f0; position: relative; overflow-x: hidden; }

/* Header */
header {
  background: linear-gradient(135deg, #4e342e, #8d6e63);
  color: white;
  padding: 3rem 2rem;
  text-align: center;
  position: relative; z-index: 1;
}
header h1 { font-size: 3rem; margin-bottom: 1rem; }
header p { font-size: 1.3rem; }

/* Section Styles */
section { padding: 4rem 2rem; text-align: center; position: relative; z-index: 1; }
section h2 { font-size: 2rem; margin-bottom: 2rem; color: #4e342e; }

/* Menu Grid */
.menu {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 2rem;
  max-width: 1000px;
  margin: auto;
}
.menu-item {
  background: #fff;
  border-radius: 12px;
  padding: 1rem;
  box-shadow: 0 4px 10px rgba(0,0,0,0.08);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}
.menu-item:hover {
  transform: translateY(-8px) scale(1.03);
  box-shadow: 0 8px 20px rgba(0,0,0,0.15);
}
.menu-item img {
  width: 100%;
  border-radius: 10px;
  margin-bottom: 1rem;
}
.menu-item h3 { margin-bottom: 0.5rem; color: #3e2723; }

/* Fade In Animation */
.fade-in {
  opacity: 0;
  transform: translateY(30px);
  transition: opacity 1s ease-out, transform 1s ease-out;
}
.fade-in.visible {
  opacity: 1;
  transform: translateY(0);
}

/* Footer */
footer {
  background: #4e342e;
  color: white;
  padding: 2rem;
  text-align: center;
  margin-top: 2rem;
  position: relative; z-index: 1;
}

/* Floating Chocolates & Sparkles */
.floating-chocolates {
  position: fixed; top: 0; left: 0; width: 100%; height: 100%;
  pointer-events: none; overflow: hidden; z-index: 0;
}
.choco {
  position: absolute;
  background: #6b3e26;
  border-radius: 50%;
  opacity: 0.7;
  animation: floatChoco linear infinite;
}
.choco.small { width: 12px; height: 12px; animation-duration: 12s; }
.choco.medium { width: 20px; height: 20px; animation-duration: 16s; }
.choco.large { width: 30px; height: 30px; animation-duration: 20s; }

.sparkle {
  position: absolute;
  width: 4px; height: 4px;
  background: #d9b38c; border-radius: 50%;
  opacity: 0.5;
  animation: floatSparkle linear infinite;
}

@keyframes floatChoco {
  0% { transform: translateY(0) rotate(0deg); }
  50% { transform: translateY(400px) rotate(180deg); }
  100% { transform: translateY(-100px) rotate(360deg); }
}
@keyframes floatSparkle {
  0% { transform: translateY(0) translateX(0); opacity:0.3; }
  50% { transform: translateY(300px) translateX(50px); opacity:0.7; }
  100% { transform: translateY(-50px) translateX(-50px); opacity:0.3; }
}

/* Random positions */
.choco:nth-child(1){left:5%;top:-50px;} .choco:nth-child(2){left:20%;top:-100px;}
.choco:nth-child(3){left:40%;top:-80px;} .choco:nth-child(4){left:65%;top:-150px;}
.choco:nth-child(5){left:80%;top:-120px;}
.sparkle:nth-child(6){left:10%;top:-20px;animation-duration:14s;} 
.sparkle:nth-child(7){left:30%;top:-50px;animation-duration:12s;}
.sparkle:nth-child(8){left:50%;top:-30px;animation-duration:16s;} 
.sparkle:nth-child(9){left:70%;top:-60px;animation-duration:18s;}
.sparkle:nth-child(10){left:90%;top:-40px;animation-duration:20s;}

/* Scroll Particles */
.scroll-particles { position: fixed; top:0; left:0; width:100%; height:100%; pointer-events:none; z-index:0; }
.scroll-particles .particle {
  position: absolute; width: 5px; height: 5px; background: #d9b38c; border-radius: 50%; opacity: 0.6; animation: drift 3s forwards;
}
@keyframes drift {
  0% { transform: translate(0,0) scale(1); opacity:0.6; }
  100% { transform: translate(var(--x), var(--y)) scale(0.5); opacity:0; }
}