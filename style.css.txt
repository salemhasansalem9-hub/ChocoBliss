/* Fonts */
body { font-family: 'Roboto', sans-serif; line-height:1.6; color:#333; background:#fff8f0; overflow-x:hidden; }
h1,h2,h3 { font-family:'Playfair Display', serif; }

/* Navigation */
nav { position:fixed; top:0; left:0; width:100%; background:#4e342e; z-index:10; }
nav ul { display:flex; justify-content:center; list-style:none; }
nav ul li { margin:0 1rem; }
nav ul li a { color:#fff; text-decoration:none; font-weight:bold; padding:1rem; display:inline-block; transition:0.3s; }
nav ul li a:hover { background:#6b3e26; border-radius:5px; }

/* Header */
header { background:linear-gradient(135deg,#4e342e,#8d6e63); color:#fff; padding:8rem 2rem 4rem; text-align:center; }
header h1 { font-size:3rem; margin-bottom:1rem; }
header p { font-size:1.3rem; font-style:italic; }

/* Sections */
section { padding:4rem 2rem; text-align:center; }
section h2 { font-size:2rem; margin-bottom:2rem; color:#4e342e; }

/* Menu */
.menu { display:grid; grid-template-columns:repeat(auto-fit,minmax(220px,1fr)); gap:2rem; max-width:1000px; margin:auto; }
.menu-item { background:#fff; border-radius:12px; padding:1rem; box-shadow:0 6px 15px rgba(0,0,0,0.1); transition:transform 0.4s ease, box-shadow 0.4s ease; position:relative; overflow:hidden; }
.menu-item:hover { transform:translateY(-10px) scale(1.03); box-shadow:0 12px 25px rgba(0,0,0,0.2); }
.menu-item img { width:100%; border-radius:10px; margin-bottom:1rem; }
.menu-item h3 { margin-bottom:0.5rem; color:#3e2723; font-size:1.3rem; }
.menu-item p { font-size:0.95rem; color:#5d4037; }

/* Order Button */
.order-btn { margin-top:0.5rem; padding:0.6rem 1.2rem; background:#6b3e26; color:#fff; border:none; border-radius:25px; cursor:pointer; font-weight:bold; box-shadow:0 3px 6px rgba(0,0,0,0.2); transition:0.3s; }
.order-btn:hover { background:#4e342e; transform:scale(1.05); box-shadow:0 5px 12px rgba(0,0,0,0.3); }

/* Fade-in */
.fade-in { opacity:0; transform:translateY(30px); transition:opacity 1s ease-out, transform 1s ease-out; }
.fade-in.visible { opacity:1; transform:translateY(0); }

/* Footer */
footer { background:#4e342e; color:#fff; padding:3rem 2rem; text-align:center; font-size:0.95rem; }

/* Floating Chocolates */
.floating-chocolates { position:fixed; top:0; left:0; width:100%; height:100%; pointer-events:none; overflow:hidden; z-index:0; }
.choco, .sparkle { position:absolute; border-radius:50%; opacity:0.7; }
.choco { background:#6b3e26; }
.choco.small { width:12px; height:12px; animation:floatChoco linear infinite 12s; }
.choco.medium { width:20px; height:20px; animation-duration:16s; }
.choco.large { width:30px; height:30px; animation-duration:20s; }
.sparkle { width:4px; height:4px; background:#d9b38c; opacity:0.5; animation:floatSparkle linear infinite; }
@keyframes floatChoco { 0%{transform:translateY(0) rotate(0deg);}50%{transform:translateY(400px) rotate(180deg);}100%{transform:translateY(-100px) rotate(360deg);} }
@keyframes floatSparkle {0%{transform:translateY(0);opacity:0.3;}50%{transform:translateY(300px);opacity:0.7;}100%{transform:translateY(-50px);opacity:0.3;}}

/* Scroll Particles */
.scroll-particles { position:fixed; top:0; left:0; width:100%; height:100%; pointer-events:none; z-index:0; }
.scroll-particles .particle { position:absolute; width:5px; height:5px; background:#d9b38c; border-radius:50%; opacity:0.6; animation:drift 3s forwards; }
@keyframes drift {0%{transform:translate(0,0) scale(1);opacity:0.6;}100%{transform:translate(var(--x),var(--y)) scale(0.5);opacity:0;}}
